namespace SimuladorMaquinaTuring.Modelo
{
    public enum Direccion
    {
        Izquierda,
        Derecha,
        NoMoverse
    };

    public enum Estado
    {
        Ejecutandose,
        Pausada,
    };
}
